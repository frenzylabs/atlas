{
  "name": "ancilla",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "services/*",
    "libs/*"
  ],
  "scripts": {
    "start": "nodemon --exec yarn lerna",
    "lerna": "lerna bootstrap && lerna run start --stream"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.5.0",
    "lerna": "^4.0.0"
  }
}
