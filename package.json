{
  "name": "atlas",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "nodemon --watch ./packages/cdn/src --watch ./packages/api/src --watch ./packages/engine/src --watch  ./packages/web/src --watch  ./packages/server/src -e ts,scss,html,tsx --exec yarn lerna",
    "lerna": "lerna bootstrap && lerna run start --stream"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.5.0",
    "lerna": "^4.0.0"
  }
}
